#include <stdio.h>

/**
 * main - Pribys the alphabet in lowercase.
 *
 * Return: Always 0
 */

int main(void)

{

	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(ch);

	putchar('\n');

	return (0);

}
